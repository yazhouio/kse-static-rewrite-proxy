name: Build Docker Images

on:
  pull_request:
  push:
    branches:
      - main
      - release/*
      - hotfix/*
  release:
    types:
      - published

permissions:
  contents: read

env:
  IMAGE_NAMESPACE: kubesphere
  IMAGE_NAME: kse-static-rewrite-proxy
  KUBESPHERE_HUB_REGISTRY: hub.kubesphere.com.cn
  SHOULD_PUBLISH: ${{ github.event_name != 'pull_request' }}

jobs:
  build-by-arch:
    name: Build image (${{ matrix.platform }})
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-latest
            platform: linux/amd64
            arch: amd64
          - runner: ubuntu-24.04-arm
            platform: linux/arm64
            arch: arm64
    steps:
      - name: Check Out Repository
        uses: actions/checkout@v6

      - name: Set Up Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log In To KubeSphere Hub
        if: ${{ env.SHOULD_PUBLISH == 'true' }}
        uses: docker/login-action@v4
        with:
          registry: ${{ env.KUBESPHERE_HUB_REGISTRY }}
          username: ${{ secrets.KUBESPHERE_HUB_USERNAME }}
          password: ${{ secrets.KUBESPHERE_HUB_TOKEN }}

      - name: Build PR Image
        if: ${{ github.event_name == 'pull_request' }}
        uses: docker/build-push-action@v7
        with:
          context: .
          file: Dockerfile
          platforms: ${{ matrix.platform }}
          push: false
          cache-from: type=gha,scope=${{ env.IMAGE_NAME }}-${{ matrix.arch }}
          cache-to: type=gha,mode=min,scope=${{ env.IMAGE_NAME }}-${{ matrix.arch }}

      - name: Build And Push By Digest
        if: ${{ env.SHOULD_PUBLISH == 'true' }}
        id: build
        uses: docker/build-push-action@v7
        with:
          context: .
          file: Dockerfile
          platforms: ${{ matrix.platform }}
          outputs: type=image,name=${{ env.KUBESPHERE_HUB_REGISTRY }}/${{ env.IMAGE_NAMESPACE }}/${{ env.IMAGE_NAME }},push-by-digest=true,name-canonical=true,push=true
          cache-from: type=gha,scope=${{ env.IMAGE_NAME }}-${{ matrix.arch }}
          cache-to: type=gha,mode=min,scope=${{ env.IMAGE_NAME }}-${{ matrix.arch }}

      - name: Export Digest
        if: ${{ env.SHOULD_PUBLISH == 'true' }}
        shell: bash
        run: |
          mkdir -p digests
          echo "${{ steps.build.outputs.digest }}" > "digests/${{ matrix.arch }}.txt"

      - name: Upload Digest
        if: ${{ env.SHOULD_PUBLISH == 'true' }}
        uses: actions/upload-artifact@v7
        with:
          name: digests-${{ matrix.arch }}
          path: digests/*.txt

  merge-manifest:
    if: ${{ github.event_name != 'pull_request' }}
    name: Merge multi-arch manifest
    runs-on: ubuntu-latest
    needs:
      - build-by-arch
    steps:
      - name: Download Digests
        uses: actions/download-artifact@v8
        with:
          pattern: digests-*
          path: digests
          merge-multiple: true

      - name: Set Up Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log In To KubeSphere Hub
        uses: docker/login-action@v4
        with:
          registry: ${{ env.KUBESPHERE_HUB_REGISTRY }}
          username: ${{ secrets.KUBESPHERE_HUB_USERNAME }}
          password: ${{ secrets.KUBESPHERE_HUB_TOKEN }}

      - name: Create And Push Manifest
        shell: bash
        run: |
          set -euo pipefail

          image_repository="${KUBESPHERE_HUB_REGISTRY}/${IMAGE_NAMESPACE}/${IMAGE_NAME}"
          if [[ "${GITHUB_EVENT_NAME}" == "release" ]]; then
            image_tags=("${image_repository}:${GITHUB_REF_NAME}")
          else
            safe_ref="${GITHUB_REF_NAME//\//-}"
            image_tags=("${image_repository}:${safe_ref}" "${image_repository}:latest")
          fi

          mapfile -t digest_files < <(find digests -maxdepth 1 -type f -name '*.txt' | sort)
          image_references=()
          platforms=()
          for digest_file in "${digest_files[@]}"; do
            arch="$(basename "${digest_file}" .txt)"
            digest="$(cat "${digest_file}")"
            image_references+=("${image_repository}@${digest}")
            platforms+=("linux/${arch}")
          done

          tag_arguments=()
          for image_tag in "${image_tags[@]}"; do
            tag_arguments+=(--tag "${image_tag}")
          done

          docker buildx imagetools create \
            "${tag_arguments[@]}" \
            "${image_references[@]}"

          primary_tag="${image_tags[0]}"
          image_digest="$(
            docker buildx imagetools inspect "${primary_tag}" \
              | sed -n 's/^Digest: //p' \
              | head -n1
          )"
          tag_list="$(printf '`%s`, ' "${image_tags[@]}")"
          tag_list="${tag_list%, }"
          platform_list="$(printf '`%s`, ' "${platforms[@]}")"
          platform_list="${platform_list%, }"

          {
            echo "## Multi-platform image"
            echo
            echo "- Tags: ${tag_list}"
            echo "- Digest: \`${image_digest}\`"
            echo "- Platforms: ${platform_list}"
          } >> "${GITHUB_STEP_SUMMARY}"
